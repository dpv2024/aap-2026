---
- name: Restart Linux server
  hosts: all
  become: true

  tasks:
    - name: Restart server
      ansible.builtin.reboot:
        reboot_timeout: 600
        connect_timeout: 10
        pre_reboot_delay: 5
        post_reboot_delay: 30
